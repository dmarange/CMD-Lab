# Dual Site Dipole Moment and Effect of the Electric Field

While in your virtual environment:
terminal:
```bash
env
```

Make required folders:
```bash
python make_runs.py
```

Submit jobs (NOTE: the job folder should be added in --):
Example
```bash
python submitalljobs.py --no2
```

Check on jobs with:
```bash
sq
```